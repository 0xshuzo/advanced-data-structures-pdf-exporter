name: Schedule Smoke Test

on:
  workflow_dispatch:
  schedule:
    - cron: "7,22,37,52 * * * *"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: |
          date -u
          echo "schedule fired"
